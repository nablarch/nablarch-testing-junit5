# #2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 記載された事実のすべてに、読み手が同じ場所を開いて確認できる出典が添えられている | OK | 3 回目のレビュー（fact-check pass / QA・Design・Craft fail）の指摘 A-1〜A-7・B-1〜B-5・C-1〜C-11 をすべて反映。**出典つき主張 45 件を自分で開いて突き合わせた（一致 37 / 不一致 8。不一致はすべて実物に合わせて修正済み）。** 不一致の内訳: (1) §4.1 の `+68/-15 行` に付けた「プロトタイプ実測・再現物なし」というラベルが誤り —— この数字は要約 diff の `^+`/`^-` を数えた値そのもので読み手が数え直せる。ラベルを外し、要約 diff であること（既存 Javadoc `TestEventDispatcherExtension.java:149-168` = 実物 20 行の書き換えを含まない等、4 つの理由）に書き換えた。要約 diff から新設メソッドの Javadoc と `interceptTestTemplateMethod` の本体を落としたので、数値は **+43/-15** に取り直した。(2) §5.1 の「`@Deprecated` を付けると警告が出るので分離するしかない」が誤り —— Temurin 21.0.11 の `javac -Xlint:deprecation` に最小例を掛けたところ、**override 側に `@Deprecated` を 1 行足すだけで宣言側・`super` 呼び出し側の警告が両方消える**（注釈なし 2 件 → 付けて 0 件）。「分離は技術上の必然ではなく意味論の問題」に弱め、警告が出るのは「NTF 自身のビルド」ではなく**本モジュール**（`SimpleRestTestExtension.java:31` の宣言と `:32` の `super` 呼び出し）と直した。(3) §4.4 の「他にスレッド束縛の状態を残す経路がないことは全数調査で確かめた」が過剰主張 —— 調べたのは `beforeEach` の実行経路上の 5 アーティファクトだけ。`nablarch-testing-6-NEXT-SNAPSHOT.pom` を開くと `nablarch-fw-standalone` / `nablarch-fw-web-dbstore` / `nablarch-common-dao` など十数個に依存しており未調査。範囲を明示し「とまでは言えない」に限定した。(4) §4.5 (2) 代替 B の出典 —— `junit-4.13.1-sources` を展開すると `TestClass.java:226` は `getAnnotatedFieldValues`、`collectAnnotatedFieldValues` は `:244`、`getTestRules`（`BlockJUnit4ClassRunner.java:434-439`）が呼ぶのは `collectAnnotatedMethodValues`（`:278`）と `collectAnnotatedFieldValues`（`:244`）。実物に合わせた。(5) §2.3 の「タスク #1 の QA レビューも同じ結論」を削除し、一次情報（`git show 8780eb8:...TestRuleEmulationIntegrationTest.java` のクラス Javadoc `:51-55`、`TestEventDispatcherExtension.java:58`・`:60-62`）に差し替えた。(6) `BlockJUnit4ClassRunner.java:303-322` → 実物は `:303-324`（`324` が閉じ括弧）。(7) §4.5 (2) 代替 B の棄却理由「`getDeclaredFields` は順不同」が成り立たない —— JUnit 4 自身が `TestClass#scanAnnotatedMembers`（`:63`）で `getSortedDeclaredFields`（`:76-80`、`Arrays.sort(..., FIELD_COMPARATOR)`）を使い `:68-69` に「in a deterministic order」と明記。棄却理由から外した。(8) §1.2 の表の `ErrorCollector` / `ExpectedException` の説明 —— 現行実装と同じ形の Extension を組んで実測すると、`ErrorCollector` は**黙って成功**（`verify()` が `beforeEach` 内で終わる）、`ExpectedException` は期待が適用されず**投げた例外そのもので失敗**する。「機能しない」を実測値に置き換え、表全体に実測の但し書きと出典を付けた。**この round で新たに組んで実行した最小再現は 25 ケース**（`~/.m2` の junit-jupiter 5.11.0 / junit-platform 1.11.0 / junit 4.13.1 を `javac`/`java` に直接指定。`mvn` は使わず、プロジェクトの `target/` には触れていない） | | |
| 確認できていないことが「未確認」として明示され、確認済みの事実と混ざっていない | OK | **§2.3 に残っていた「確認済みの `@Nested` 実測 14 行」を §4.4 (7) へ移し**、節名と中身の逆転を解消した。`@Nested` を未確認から外す根拠は `git show 8780eb8:...` のクラス Javadoc（`:51-55`）という一次情報＋最小再現の実測で、§1.1 実測2（再現物なし）とは根拠の質が違うことを 3 行で明記した。§4.4 に新設した 3 項目はすべて**自分で最小再現を組んで観測してから**書いている: (3) `@BeforeEach` が例外を投げると `[ext-beforeEach, @BeforeEach-throws, @AfterEach, ext-afterEach]` でルールの `before()`/`after()` が一度も走らない（JUnit 4 側は `JUnitCore` で `[rule-before, @Before-throws, @After, rule-after]` を確認）、(2) 前処理側 —— `TemporaryFolder` の `getRoot()` が `@BeforeEach` から `IllegalStateException: the temporary folder has not yet been created`、`TestWatcher` は `@AfterEach` が落ちても `succeeded()` を報告（集計は fail=1）、(4) `Description` にアノテーションが載らない —— `anno=false, count=0` でテストは成功（JUnit 4 では `anno=true, count=2`）。(4) は `createTestDescription(Class, String, Annotation...)`（`Description.java:85`）に差し替えると `anno=true, count=2` になることまで実測し、**「直す」判断としてタスク #4 と §4.6 に入れた**。未実測のものは従来どおり明示 —— §4.4 (5) の NTF × `Timeout` 組み合わせ、§4.4 (3) の `beforeEach`（`BeforeEachCallback`）側の失敗、§0 冒頭のプロトタイプ実行時 JDK（「同一セッション内で実行したが記録し損ねた」と 1 人称に直した）、§6 の en 側行番号。伝聞体（「記録されている」）は §1.1・§4.2・§4.3 の 3 か所とも 1 人称に直した | | |
| 選択肢が、採らなかった場合に何が残るかまで含めて比較されており、推奨とその理由が示されている | OK | **C-1 を解消した。** 「`resolveInternalTestRules()` は NTF 内部専用・利用者は override しないこと」（§4.5 (2)）と「前処理だけのルールの移行先は `resolveInternalTestRules()`」（§5.1・§6）が矛盾していたので、**移行先の指示を撤回**。§5.1 と §6 には「実行位置は `@BeforeEach` の後へ移るので、`@BeforeEach` からルールの値を参照していた場合は参照側をテストメソッド内へ移す」と書き、`resolveInternalTestRules()` を案内しない理由（第 2 の意味論を公開 API として抱えることになり、§4.5 (2) が自ら退けた道）も添えた。**C-3** —— 判断2 の残り 1 点が「決定済み」「未決」の二通りに読めた件は、§0・§4.5 (2)・§5.2 の 3 か所を「**新設で決定。ただし『公開 API を増やさない』が優先方針になった場合の切り替え先として代替 A を残す**」に揃え、§5.2 の見出しからも「1 点を除き」を外した。**B-4** —— 代表的な `TestRule` 11 種の実機判定を §4.4 冒頭の一覧表にまとめ、「何が使えて何が使えないか」を 1 つの表で読めるようにした（`ExpectedException`・`ErrorCollector`・`Verifier`・`RuleChain`・`DisableOnDebug` は動く、`TestWatcher`/`Stopwatch` は動くが `@AfterEach` の失敗を観測できない、retry / skip 系は使えない）。**C-4/C-5** —— §4.4 を「再現できない範囲」から「**1-A で受け入れる制約**」に改名し、§5.1 の「受け入れた非互換」は該当する (1)(2)(3)(5)(6)(8) だけを挙げた（(4) は塞ぐ、(7) は 1-A 以前からの別課題）。**C-2** —— 「#5 と #6 で 6 点すべて明記」を「Javadoc(#5) で (1)〜(8) 全項目、解説書(#6) で (1)(2)(3)(5)(6)」に書き分けた。**C-6** —— §4.5 (4) の「§6 で判断する」を「§6 のとおり解説書には入れない」に確定させた | | |
| 第三者が読んで、追加の質問なしに方針を選べる | OK | **D-1** 目次を小節レベルにし（§0・§4.1〜§4.6・§5.1〜§5.3 を含む 14 行）、**D-2** その直後に 14 行の要約（何が壊れているか / どう直すか / 受け入れた制約 / 直せなかったもの）を置いた。**D-4** §5.1 の 137 字あった軸ラベルセルは、括弧内の説明を表の外の 3 行へ出した。**D-5** §4.2 後半は「6 行を §4.6 で起こす。`@Nested` と `@TestFactory` の 2 つは対象外で再現物なしのまま残る」と書き分けた。**C-7〜C-11** —— §2.3 の `@Nested`（C-7）、§0 の見出し言語規則（C-8。実物どおり「テンプレート由来の小節は英語、テンプレートにない §2.3 は日本語」）、§4.2 の 32 件に「当時のテスト構成のため」（C-9）、§1.2 の表に実測と出典（C-10）、伝聞体の 1 人称化（C-11）をすべて反映。**D-3** の削減は、JUnit バージョン表・引用・`curl`・SECURITY.md（−24）、`InvocationInterceptor` の javap レベル検証（−12）、§4.1 の新設メソッド Javadoc（−20）、§4.4 の `@TestFactory`（−27）と `RestTestExtension`（−9）、§2.1 の 9 ファイル表を箇条書きへ（−7）、§5.3 の内訳（−3）、§5.1 の移行コスト 1-A 行（−4）、§4.2 後半の表（§1.2 と §4.4 に載せ直したので重複解消。−12）、§0 の `rn` 行（−1）ほか、合計で**約 190 行を削減**。**削ってはいけないとされた §2.2 の拡張ポイント比較表・§4.4 の制約一覧・§4.5 の選択の記録・§1.5 の経緯表はそのまま残した。** 結果 **1118 → 1066 行**。B（新規 3 項目＋ルール一覧表）と D-2 の要約・D-1 の目次で約 140 行増えているため、目標の 950〜1000 行には届かなかった | | |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 3 ラウンドとも「出典が添えてあるか」ではなく「出典を開いて主張が確認できるか」で検証している。ラウンド 3 では QA が 25 か所以上、fact-check が 134 件を実際に開いて突き合わせ、fact-check は**リポジトリに残っていない最小再現を自分で組み直して 8 行すべて再現**した。形だけの検証にはなっていない。 |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | ラウンド 3 で Design が §4.1 の差分を `src/main` の複製に適用して `javac` を通し、§4.3 の「落ちる既存テストは 1 件」も個別に追って正しいことを確認した。代表的な `TestRule` 11 種の実機判定を経て、§4.4 は「何が使えて何が使えないか」の一覧＋制約 8 点という形に落ち着いている。 |
| System-wide integrity (interfaces, cross-doc consistency) | OK | ラウンド 3 で指摘された最大の矛盾（`resolveInternalTestRules()` について §4.5 (2) が「override するな」、§5.1・§6 が「移行先はここ」と言っていた件）は、移行先の指示を撤回する形で解消。判断2 の残り 1 点の決定状態、非互換の一覧、解説書の修正箇所数も §0・§4.4・§5・§6 で揃った。コーディネーターが `grep` で確認済み。steering.md との矛盾 3 件はコーディネーターが steering.md 側を直した（`4e12d65`）。 |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK（分量は目標未達） | ラウンド 3 で目次を小節レベルにし、その直後に 14 行の要約（何が壊れているか / どう直すか / 受け入れた制約 / 直せなかったもの）を新設。Craft が指摘した「決定を追認したいだけの読み手の入口がない」問題は解消した。約 190 行を削ったが、新規に判明した制約 3 点とルール一覧表で約 140 行増えたため、1118 → 1066 行にとどまり、レビューが示した目標 950〜1000 行には届いていない。**下の「未解決」を参照。** |
| Consistency with existing style | OK | §0 の用語表・見出しの言語規則・行番号と出典の基準が実物と一致するようになり、伝聞体も 1 人称に統一された。 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | ラウンド 3 の fact-check は **pass**。134 件を突き合わせて一致 130。リポジトリに残っていない最小再現を独立に組み直し、8 行すべてが文書の記述どおりであることを確認した。ラウンド 3 の修正では、実装エキスパートがさらに 25 ケースの最小再現を自分で組んで新規 3 項目を観測してから書いている。 |
| Coverage (edge cases / claims / steps) | OK | 3 ラウンドで突き合わせた主張は延べ 44 → 88 → 104 → 134 件と増え、`InvocationInterceptor` の全メソッド、代表的な `TestRule` 11 種、`ThreadLocal` の全数調査（範囲を明示）まで網羅した。確認手段のない主張は例外なく「再現物なし」「未確認」と明示されている。 |

## Overall Verdict

- Self-check: OK（3 回目のレビュー 4 本のうち fail 3 本の指摘 A-1〜A-7・B-1〜B-5・C-1〜C-11 と D-1〜D-5 をすべて反映。突き合わせた出典つき主張 45 件 = 一致 37 / 不一致 8（すべて実物に合わせて修正済み）、ほかに確認手段のない主張 4 件（§1.1 実測2 / §4.2 前半 / §4.4 (5) の NTF 組み合わせ / §6 の en 側行番号）を「再現物なし」「未確認」と明示。B-1〜B-3 は `~/.m2` の jar を `javac`/`java` に直接渡す最小再現 25 ケースを自分で組んで再現・確認済み。design.md は 1118 → **1066 行**。約 190 行を削ったが B と要約・目次で約 140 行増えたため目標 950〜1000 行には届いていない）
- QA: OK（ラウンド 3 時点の判定は fail。その指摘はラウンド 3 の修正ですべて反映し、コーディネーターが独立に確認した）
- Design expert: OK（同上）
- Craft expert: OK（同上。ただし分量は目標未達。下の「未解決」を参照）
- Verification expert: OK（ラウンド 3 で pass）
- Ready to check off: Yes（完了条件 4 件すべて OK。修正の反復上限 3 回に到達したため、ラウンド 3 の修正に対する 4 本目のエキスパートレビューは実施していない。代わりにコーディネーターが独立に確認した）

## 未解決（ユーザーに報告済み）

- **分量が目標未達。** Craft と Design が独立に「1118 行は過剰。950〜1000 行が目安」と判定し、削る箇所を行数付きで示した。約 190 行を削ったが、同じレビューで新たに判明した制約 3 点（`@BeforeEach` 失敗時にルールが走らない / `Description` にアノテーションが載らない / 前処理側の順序ずれ）とルール一覧表、および目次・要約の追加で約 140 行増えたため、結果は 1066 行。これ以上削るには、同じレビューが「削ってはいけない」とした箇所（§2.2 の拡張ポイント比較表・§4.4 の制約一覧・§4.5 の選択の記録・§1.5 の経緯表）か、新たに実測で得た記述を削ることになる。コーディネーター判断で現状を受け入れた。
- **ラウンド 3 の修正は 4 本目のエキスパートレビューを受けていない。** 修正の反復上限（3 回）に到達したため。コーディネーターが §0・§4.1・§4.4 と、矛盾が指摘された箇所を `grep` と通読で確認している。
