# #6 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 差分案が、そのまま別リポジトリへ適用できる粒度で書かれている | OK | **主成果物を `git apply` できる unified diff（126 行、CR を含まない）にした**（§2）。**引用の二重管理をやめ、§3 からは追加内容の再掲を落として §4 と形を揃えた**（変更前の原文の引用だけ残した。約 50 行減）。§1.4 に、CRLF を保ったまま当てる手順（切り出し範囲の明示 → 一時的に LF 化 → `git apply` → CRLF に戻す → `pip install -r requirements.txt` を含む textlint）をコマンドで書き、§1.5 に受け取った人が実施する検算（488 行 / CRLF / `78 insertions(+), 12 deletions(-)` / `core.autocrlf=true` で CR が落ちた場合の気づき方）を表として置いた。**この diff を document-patch.md から抽出し、`5391d5c` の複製へ実際に当てて確認した**（`git apply` 成功、ja / en とも 488 行 CRLF、`git diff --stat` が 78/12、`docutils` 0.15.2 / 0.22.4 の双方で system message 0 件、`npx textlint` の指摘は原文と同じ 3 件）。作業は一時ディレクトリで行い、`nablarch-document` には書き込んでいない | | |
| 修正後の記述どおりに実装すれば動作することが、#4 の成果物で裏づけられている | OK | §5 に、差分案の各記述と本リポジトリの恒久テストとの対応表を置いた。**レビューで指摘された 2 件の裏づけの欠落を直した。** (1) 解説書が名指ししていた `TestWatcher` は、`javap` で確認した結果 JUnit 4.13.1 の `Stopwatch`（`implements TestRule`）とは無関係な兄弟クラスであり、実測している恒久テスト（`StandardTestRuleIntegrationTest.java:173-183` の `RecordingStopwatch extends Stopwatch`）が使っているのは `Stopwatch` なので、解説書の文面を `Stopwatch` に差し替えた。(2) `TimeoutRuleIntegrationTest` のフィクスチャが `new ArrayList<>(super.resolveTestRules())` という**修正前**の形だったので、修正後のコード例と同じ `new ArrayList<>()` に変更し（`:126`）、Javadoc も整合させた（`mvn -o clean test` で 62 件全件成功）。**唯一恒久テストがない記述（タイムアウト時の挙動）は、`FailOnTimeout.java:176` の `thread.interrupt()` を自分で確認したうえで最小再現を書いて両分岐を実測し、実測どおりの文面に書き換えた**。**この 2 文に恒久テストを追加しない判断と理由も §3 修正2 に残した**（裏づけは `FailOnTimeout.java:135` / `:176` という一次情報で読み手が確かめられること、design.md §4.6 が「タイムアウト成立とスレッド割り込みのタイミング依存で不安定になりやすい」ことを理由に恒久テストにしないと決めていることの 2 点）。**裏づけ表の `StandardTestRuleIntegrationTest.java:93-111` は実際は `:93-110` だったので直した**（111 行目は空行） | | |
| ja / en の内容が一致している | OK | §2 の diff で ja / en が同一の構造（修正1 −1 行、修正2 +10 行、修正3 +21 行、修正4 +3 行、箇条書き 4 項目、警告 2 段落 6 文、1 項目目の中の `.. tip::` 1 個）になっていることを確認した。ja / en とも 455 行 → 488 行、`git diff --stat` は両ファイルとも `45`（+39 −6）。§4 に「ja 修正N の理由に同じ」の対応表を置き、en だけをレビューする人が §3 へ戻らなくても済むようにした。en は ja の直訳ではなく既存の英文に合わせたうえで、`Note that` の乱用（追記分 5 回。既存ファイル全体では 2 回）を 1 回に減らした。**今回さらに 3 点を直した。** (1) `because it executes the test method in a separate thread` の `it` が直前の `DbAccessTest` を指しうるので `because ``Timeout`` executes ...` にした。(2) 不可算名詞 `code` の並列に複数動詞が付いていた `code in ..., and code that ..., stop working` を `..., both **stop working ...**` にした。(3) ja が「ので注意すること」で喚起している 1 か所に対応する英文がなかったので、同じファイルの既存ペア（ja rst:96「値は設定しないこと」/ en rst:96 `Don't set any value to the field`）に倣い `Be careful if you leave the release of resources to the rule: ...` と命令形にした | | |

## Method（検証方法の適用確認）

### 一次情報で確認したこと（自分で実行した）

| 確認したこと | 手段と結果 |
|---|---|
| `Stopwatch` は `TestWatcher` を継承していないこと | `javap -cp junit-4.13.1.jar org.junit.rules.Stopwatch` → `public class org.junit.rules.Stopwatch implements org.junit.rules.TestRule`。`org.junit.rules.TestWatcher` は `public abstract class ... implements TestRule` で、両者は無関係な兄弟。**解説書が名指ししていた `TestWatcher` を `Stopwatch` に差し替えた** |
| タイムアウト時にテストメソッドが打ち切られるかどうか | `junit-4.13.1-sources.jar` を展開し `FailOnTimeout.java` を開いて `grep -n` → `:135` が `thread.join(1)`、**`:176` が `createTimeoutException()` の中の `thread.interrupt()`**。前版はこの `:176` を見落としていた。最小再現（タイムアウト 300 ミリ秒、本体 1500 ミリ秒）を `src/test` に一時的に置いて実行し、**割り込みを無視するビジーループ → `[body-start, afterEach, body-end interrupted=true]`**、**`Thread.sleep` → `[body-start, body-INTERRUPTED, afterEach]`** を実測。実行後に削除した |
| 「テスト本体」がこの解説書群にあるか | `git grep -o "テスト本体" 5391d5c -- 'ja/**' \| wc -l` → **0**。`テストメソッド` は **70**。`git grep -in "body of the test" 5391d5c -- 'en/**'` → **0 件**。「テストメソッド」/ `the test method` に統一した |
| rst のインラインマークアップと全角括弧の空白 | `docutils` に 9 パターンを食わせて解析結果を比較した。**手元の 0.22.4 だけでなく、反映先が `requirements.txt` で固定している 0.15.2 でも同じ結果**であることを確かめた。**インラインマークアップ終端の直後の `（` は、間の半角スペースが必須**（詰めると `` `` `` では WARNING、`:java:extdoc:` では**エラーを出さないまま生テキストに飲み込まれる**）。`（` の直後と `）` の直前は詰めてよい。表を document-patch.md §1.3 に残した |
| `**...** 。` の慣行 | **前版で `**...**。` と詰めたが、その判断の根拠にした corpus 調査が `**` だけに限られていたので、調査をやり直して差し戻した。** 同じ「インラインマークアップ終端＋句点」を `` ` `` でも数えると、`git grep` → `5391d5c` の ja 配下は **`` `` `` + 半角スペース + `。` が 126 件、詰めた形が 15 件**。`**` の 2 件 / 0 件と合わせて **128 対 15 で「空白を入れる」が慣行**である。前版が根拠にした rst:91 は `` ` `` の直後が `）` であって句点ではなく、句点側の判断の根拠にならなかった。**`**...** 。` に戻し、件数の表を document-patch.md §1.3 に置いた** |
| textlint のゲート | `git show 5391d5c:.textlintrc` と `:.textlint/conf/prh.yml` を読んだ（`preset-ja-technical-writing` の `max-ten` 3 個・`max-kanji-continuous-len` 8 文字は有効、`sentence-length` などは無効化。prh は 30 行程度の表記ゆれ辞書）。**`5391d5c` の複製で `npm ci` し、`textlint-plugin-rst` が要求する `rst2ast`（`docutils-ast-writer==0.1.2`）を venv に入れて実際に `npx textlint` を実行した**（下表） |
| `.. tip::` の書式 | `awk` でディレクティブ行の次の行を全件表示 → **`tip` は 6 か所すべて空行なし**、`warning` は 1 か所だけで空行あり。当初 `tip` にも空行を入れていたので直した（この修正で 486 行 → 485 行） |
| `TestRuleEmulationIntegrationTest` が表明していること | `sed -n '100,140p'` で実物を確認 → このテストクラスに `@BeforeEach` はなく、`@AfterEach` での検証だけ。裏づけ表の「`@Test` 以外の経路でも同じ」を「**テストメソッドを包むことは同じ**」に限定し、`@BeforeEach` との前後関係は表明していないと書いた |

### 差分案の適用検証

**document-patch.md §2 の diff ブロックを `awk` で抽出し、`git archive 5391d5c` で取り出した複製へ §1.4 の手順どおりに当てた。**
`nablarch-document` には一切書き込んでいない（使ったのは `git show` / `git ls-tree` / `git archive` / `git rev-parse` のみ）。

| 手順 | 結果 |
|---|---|
| 抽出した diff と生成した diff の一致 | `cmp` → 一致（126 行、CR 0 個） |
| `git apply --whitespace=nowarn` | **成功**（ja / en とも） |
| `--whitespace=nowarn` なしの `git apply` | **警告も出ずに成功**。2 スペースだけの空行 13 か所はいずれもハンクの外にあり、フラグは無害な保険にすぎなかった。**§1.4 手順 3 の理由書きを事実に直した** |
| 結果の一致 | `cmp` → 本差分案が想定する内容と**バイト単位で一致** |
| 行数 | ja / en とも **455 行 → 488 行**（−1 +10 +21 +3 = +33） |
| `git diff --stat` | `2 files changed, 78 insertions(+), 12 deletions(-)`（1 ファイルあたり +39 −6） |
| 改行コード | `file` → 適用後も `with CRLF line terminators` |
| `docutils` による構文解析 | **0.15.2（反映先の `requirements.txt` の固定）と 0.22.4 の両方**で、原文 ja / en・適用後 ja / en の 4 ファイルとも **system message 0 件**（`:java:extdoc:` / `:ref:` はダミーのロール、`contents` / `code-block` はダミーのディレクティブとして登録。`code-block` を登録しないと Pygments の有無に依存した警告が出て比較にならない） |
| `docutils` の `report_level=1` | **原文・適用後とも INFO 2 件**（rst:1 と rst:46 の未参照 hyperlink target。他ファイルから `:ref:` で参照されるラベル）。件数も内容も原文と変わらないので結論は変わらないが、「0 件」が既定の `report_level=2` での話であることを §1.5 / §1.7 に明記した |
| 箇条書きの中に置いた `.. tip::` の構造 | doctree を出して確認 → 0.15.2 / 0.22.4 とも **`item1: ['paragraph', 'tip']`**、`item2`〜`item4` は `['paragraph']`。tip が制約 1 点目の `list_item` の子になっている |
| `npx textlint` | 原文 ja **error 3 件**（`prh`「利用 => 使用」rst:56 / 324 / 433）→ 適用後も**同じ 3 件**（rst:56 / 324 / **466**、行番号が +33 ずれただけ）。原文 en **0 件** → 適用後 **0 件**。**追加分が増やした指摘は 0 件**。**なお、前版の §1.6 は「本差分案は『利用者』しか使っておらず除外対象に当たる」と書いていたが、これは事実誤認だった。** `git grep` で確認したところ**追記した rst には「利用」を含む語が 1 つもなく**、適用後に「利用」が出る 3 行はすべて原文由来である。§1.6 をその趣旨に直した |

### レビュー指摘への対応

| 指摘 | 対応 |
|---|---|
| A-1 `TestWatcher` を実行するテストがない | `javap` で確認のうえ、解説書の文面と裏づけ表を `Stopwatch` に差し替えた |
| A-2 修正後のコード例を実行する恒久テストがない | `TimeoutRuleIntegrationTest.java:126` を `new ArrayList<>()` に変更し、Javadoc に「基底実装が空のリストを返すため、解説書の例と同じく `super.resolveTestRules()` をベースにしない」と書いた。`mvn -o clean test` → **Tests run: 62, Failures: 0, Errors: 0, Skipped: 0 / BUILD SUCCESS** |
| B タイムアウト時の記述が条件付きでしか成り立たない | `FailOnTimeout.java:176` を自分で確認し、最小再現で両分岐を実測。修正2 / 修正2en を「割り込みが行われるだけである」「割り込みに反応しない処理を実行している場合、テストメソッドが動き続けたまま事後処理が実行される」に書き換えた。**旧 §5（レビュー依頼時に伝えること）は削除し、裏づけ表の 1 行と §3 修正2 の実測表に統合した**（未実測を前提にした節だったため、実測後は前提ごと成り立たない） |
| C 行数の申告が誤っている | 数え直した。**前版の文面のままなら正しい内訳は −1 +7 +16 +3 = +25（455 → 480 行）**であり、「−2」「18 行」「+26 だが実質 +25」がいずれも誤りだった（修正2 の「+7」は前版の文面では正しい）。**本版は E-5 / E-6 / E-7 で文面自体が増えたため、内訳は −1 +8 +20 +3 = +30、行数は 455 → 485 行になる**（警告が 4 文 → 5 文で 8 行、制約リストに `.. tip::` と締めの 1 段落が付いて 20 行）。§1.5 は「機械的に読み取り検証した」という表明ではなく、受け取る人の検算手順に書き直した |
| D-1 修正3 の見出しと本文の食い違い | 主成果物を diff にしたので位置の記述は 1 か所になった。見出しを「rst:419 の空行の後」に合わせた |
| D-2 修正2 がコード例と説明の間に挟まっている | 説明（rst:416-418）の後・制約リストの前に移した |
| E-1〜E-9 | (1) 「事前処理」の 3 つ目の意味を `@BeforeEach` に置換。(2) 「テスト本体」→「テストメソッド」（`git grep` で前例 0 件を確認）。(3) 修正4 の事実の再掲をやめて 1 項目目を参照。(4) rst の余分な空白を docutils で確認しながら除去（必須の 1 か所は残した）。(5) `Timeout` × DB の「静かに成功する」を `**...**` で強調して他 2 件と揃えた。(6) `getRoot()` の一事例を `.. tip::` に逃がした。(7) 「JUnit 5 に同等の機能がある場合はそちらを使うこと」を追加。(8) en の `Note that` を 5 回 → 1 回に減らし、直訳的な 3 か所を直した。(9) 裏づけ表の `TestRuleEmulationIntegrationTest` の行を「テストメソッドを包むことは同じ」に限定した |
| F-1 `git apply` できる diff がない | §2 に unified diff を置き、散文は §3 / §4 に根拠として残した。抽出して実際に当たることを確認した |
| F-2 textlint のゲート | `.textlintrc` と `prh.yml` を読み、実際に `npx textlint` を実行した。§1.4 の手順 6 と §1.6 に結果を書いた |
| F-3 §1.5 が筆者の記録になっている | 「適用後に確認すること」の表に書き直し、485 行を §1.4 の手順 5 にも出した |
| F-4 §3（en）に理由がない | §4 に「ja 修正N の理由に同じ」の対応表を置いた |
| F-5 待つべきリリースのバージョン | `pom.xml:9` が `6-NEXT-SNAPSHOT` で、番号は**未定**。§1.1 に「未定であること」と「ゲートすべきは前提実装のコミットではなくリリースの公開であること」を明記した |

### 再レビュー後の最終ラウンドで直したこと

| 指摘 | 対応 |
|---|---|
| §1.6 の「本差分案は『利用者』しか使っていない」が事実誤認（3 本すべてが指摘） | `git grep` で**追記した rst に「利用」を含む語が 1 つもない**ことを確認し、「追記した rst には『利用』を含む語を 1 つも使っていない」の趣旨に直した |
| `**...**。` と詰めた判断の根拠だった corpus 調査が狭すぎた | `` ` `` まで含めて数え直し（**128 対 15**）、`**...** 。` に戻した。§1.3 の記述も件数の表に差し替えた |
| 修正4 の「基底実装は空のリストを返す」が読者から遠すぎる | 説明（rst:416-418）の**直後・警告の前**へ上げた。制約とその帰結（移行手順）は制約リストの後ろに残した。副次効果として「JUnit 5 に同等の機能があればそちらを」が節末近くに寄った。ja / en とも |
| `.. tip::` が制約 4 点目に係ると読める | 箇条書きの**外ではなく 1 点目の項目の中**（本文と同じ 2 スペースのインデント）へ移し、doctree で `item1` の子になることを確認した |
| 「上述の制約の1点目のとおり」が序数によるクロスリファレンス | 「ルールが包むのはテストメソッドの実行だけであるため」と内容で書いた（en も同様） |
| warning に「では何を使えばよいか」がない／前半と後半で話が違う | 末尾に「JUnit 5 の `@Timeout` は既定でテストメソッドを別スレッドで実行しないため、この問題は起きない」を 1 文足し（`TestEventDispatcherExtension.java:176` の Javadoc が出典）、`Timeout` 一般の性質である後半 2 文を独立した段落にした。ja / en とも |
| 「ので注意すること」が 30 行に 3 回／ja の 3 か所の書式が不揃い／en に対応する喚起がゼロ | 「静かに壊れる」3 か所を `**……** 。` で統一し、「ので注意すること」を 3 回 → 1 回に減らした。残る 1 回に対応する en を命令形（`Be careful if ...`）にした |
| en の `it` の指示対象／`code` の並列に複数動詞 | `because ``Timeout`` executes ...`、`..., both **stop working ...**` に直した |
| §3 が §2 の追加内容を 42 行そのまま再掲している | §3 から追加内容の引用ブロックを落として §4 と形を揃えた。**変更前の原文の引用は残した**（受け取った人が「何を置き換えるのか」を確かめる材料になるため） |
| 裏づけ表の行番号／`--whitespace=nowarn` の理由／切り出し範囲／Python 環境／`core.autocrlf`／挿入位置の変更理由／`report_level` | `:93-111` → `:93-110`、フラグの理由書きを事実に、手順 1 に切り出し範囲を明示、手順 6 に `pip install -r requirements.txt` を追加、§1.5 に `core.autocrlf=true` の注意を追加、§3 修正2 に design.md からの挿入位置変更の理由を追加、§1.5 / §1.7 に `report_level` の条件を明記 |
| 「タイムアウト時は割り込みが行われるだけ」の 2 文に恒久テストを追加するか | **追加しない**と判断し、理由（一次情報で読み手が確かめられること／design.md §4.6 の判断と整合すること）を §3 修正2 に 1 段落で残した |

### 書き込みの範囲

`~/work/nablarch/nablarch-document` には**一切書き込んでいない**。B の最小再現は `src/test` に一時的に置いて実行後に削除し、
`git status` で残っていないことを確認した。design.md と steering.md は変更していない。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 「差分案が書けているか」ではなく「そのまま当てられるか／記述どおりに実装すれば動くか」で検証している。3 本とも `5391d5c` の複製へ実際に `git apply` して行数・CRLF・docutils を確かめ、裏づけ表の全行を実物で照合した。1 ラウンド目は QA が「修正後のコード例を走らせる恒久テストがない」「解説書が名指しする `TestWatcher` を実行するテストがない（`Stopwatch` は `TestWatcher` を継承していない）」を突き止めており、形だけの確認になっていない。 |

## Expert Reviews (axes the task needs)

### Craft Expert (writing / documentation)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 主成果物を `git apply` できる unified diff にし、散文は根拠に回した。反映先リポジトリの textlint ゲート（`.textlintrc` + `prh.yml`）を Craft が発見し、実際に `npm ci` と `rst2ast` を用意して実行、追加分による新規指摘 0 件を確認している。docutils も反映先が固定する 0.15.2 と 0.22.4 の両方で検証済み。 |
| Consistency with existing style | OK | `.. tip::` の空行なし書式、`.. warning::` の使いどころ、`:java:extdoc:` ロール、箇条書きの作法を既存 rst と突き合わせて確認。「テスト本体」が corpus に 0 件（既存は「テストメソッド」70 件）であることも実測して統一した。**1 ラウンド目の「`** 。` を詰める」という判断は、2 ラウンド目に corpus 調査の対象が狭すぎたことが判明して撤回されている**（`` ` `` まで広げると 128 対 15 で空白を入れるのが慣行）。 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (claims verified / diff applied / sources opened) | OK | 2 ラウンドで延べ 157 件の主張を突き合わせ（78 件 → 79 件）。最終ラウンドは不一致 1 件（§1.6 の事実誤り）のみで、それも修正済み。unified diff と散文をバイト単位で突き合わせ、変更前ブロックが `5391d5c` の実物と一致することも確認している。 |
| Coverage (edge cases / claims / steps) | OK | 裏づけ表の全行について、参照先の行番号・テスト名・実行ログ・例外メッセージを実物で照合。**恒久テストがない唯一の主張（タイムアウト時の割り込み）は Verification が自分で最小再現を組んで実測し、差分案の記述が条件つきでしか成り立たないことを突き止めた** —— `FailOnTimeout.createTimeoutException()` の `thread.interrupt()`（`:176`）の見落としで、design.md §4.4 (5) にも波及していたため両方を訂正した。 |

## Overall Verdict

- Self-check: OK（完了条件 3 件すべて OK）。**主成果物を `git apply` できる unified diff にし、`5391d5c` の複製へ実際に当てて確認した**（488 行 / CRLF / `git diff --stat` 78/12 / `docutils` 0.15.2・0.22.4 とも system message 0 件 / textlint の新規指摘 0 件）。**再レビューで出た事実の誤りは 2 件とも自分で確認して直した** — §1.6 の「『利用者』しか使っていない」は `git grep` で**追記した rst に「利用」が 1 件もない**ことを確認して趣旨ごと差し替え、`**...**。` と詰めた判断は corpus を `` ` `` まで広げて数え直し（**128 対 15 で空白を入れるのが慣行**）`**...** 。` に戻した。読み順は「基底実装は空のリストを返す」2 行を説明の直後へ上げ、`.. tip::` を制約 1 点目の項目の中へ移し（doctree で `item1: ['paragraph', 'tip']` を確認）、序数によるクロスリファレンスを内容の記述に置き換えた。warning には逃げ道の 1 文（Javadoc:176 が出典）を足して 2 段落に分け、「静かに壊れる」3 か所の書式を揃え、en 側にも対応する喚起を入れた。§3 の 42 行の再掲は落として §4 と形を揃えた。**恒久テストを追加しない 2 文については、判断と理由（一次情報で確かめられること／design.md §4.6 との整合）を文書に残した**。実測できていない記述は残っていない
- QA: OK（2 ラウンド目で pass。前回指摘 6 点すべて解消）
- Craft expert: OK（2 ラウンド目で pass（条件付き）。前回指摘 14 点中 12 点解消、残り 2 点も最終ラウンドで対応）
- Verification expert: OK（2 ラウンド目で pass。79 件中不一致 1 件、最終ラウンドで対応）
- Ready to check off: Yes（完了条件 3 件すべて OK。レビュー 3 軸すべて pass）

## 本リポジトリでは完結しないこと

`document-patch.md` は**差分案**であり、反映先は別リポジトリ `nablarch/nablarch-document`（`ja/` と `en/` の `06_TestFWGuide/JUnit5_Extension.rst`）。本セッションでは作成までとし、実際の PR は出していない（steering.md の Rules による）。反映は本モジュールのリリース公開後に行う必要があり、そのバージョンは `pom.xml` が `6-NEXT-SNAPSHOT` のため**未定**。
